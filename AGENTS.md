# 工程通用约束

本文件适用于当前目录下的所有工程及其子目录。

## 通用打包规则

当用户要求打包某个工程时，必须遵守以下规则：

- 以用户指定或当前正在维护的活跃工程目录作为打包源，不混入旁边的备份、历史版本或其他工程。
- 生成 ZIP 压缩包，压缩包内保留源工程的顶层目录。
- 压缩包默认存放在源工程的上一级目录；用户指定其他位置时按用户要求执行。
- 必须排除任意层级中名为 `.git`、`docs`、`tests` 的目录及其全部内容。
- 必须排除任意层级的所有 `*.log` 文件，扩展名匹配不区分大小写。
- 不得覆盖已有压缩包；同名时依次使用 `_2`、`_3` 等最小可用编号。
- 打包完成后必须读取压缩包目录并验证：
  - 被排除的目录和 `.log` 文件数量均为 0。
  - 工程的主要源码、README 和构建入口或工程文件仍然存在；具体关键文件应根据当前工程实际结构确定。
- 最终报告压缩包的完整路径、文件大小、文件条目数和 SHA256。

除非用户明确修改本规则，否则当前目录下其他工程也统一按此规则打包。

## Git 提交规则

当用户要求提交当前工程时，必须遵守以下规则：

- 默认只提交当前正在维护的活跃版本，不提交旁边的备份目录、历史版本或其他工程；用户明确指定时除外。
- 必须忽略任意层级中名为 `.git`、`docs`、`tests` 的目录及其全部内容。
- 必须忽略任意层级的所有 `*.log` 文件，扩展名匹配不区分大小写。
- 被忽略的目录和文件只是不纳入提交，不得因此删除或修改。
- 创建提交前必须检查暂存区，确认上述历史版本、被忽略目录和 `.log` 文件均未被暂存。
