本示例使用的硬件资源：
USART TX      --   PA2  
USART RX      --   PA1

USART通信参数配置:
    -- 波特率:   115200
    -- 字符长度：8bits
    -- 停止位：  1bit
    -- 奇偶校验：无
    -- 硬件流控：禁止


本示例的功能流程：
1、上电配置系统时钟为PLL（48MHz）；
2、初始化USART使用的GPIO；
3、初始化USART；
4、开始中断发送和中断接收，使用中断方式发送8个数据；
5、在接收中断中读取接收数据。
