# Led9100_8P README 增量更新设计

## 目标

以当前工作区中的固件源码为唯一事实来源，更新 `Led9100_8P/README.md`，让文档准确描述当前默认构建配置、运行流程、PWM 输入输出逻辑、串口命令和开发注意事项。

## 更新策略

保留现有 README 的章节结构和已有有效说明，只做增量校正与必要补充，不重写整个文档，也不修改任何固件源码、工程配置或历史版本目录。

重点校正当前源码已经变化的内容：

- 默认封装配置为 `TSSOP20 = 0`，即 SOP8 路径。
- 默认芯片配置为 `CHIP_VER = CHIP_9100`。
- TIM3 输入滤波参数为 `DET_CNT = 4`、`BIT_SHIFT = 2`，即 4 次平均。
- 重新核对默认配置下实际启用的 PWM 输出、测试 PWM、LED、定时器通道和中断流程。
- 以当前源文件中的宏、初始化调用、命令解析和 ISR 实现校正 README，其余历史说明只有在仍适用时才保留。

## README 内容结构

README 继续覆盖以下内容：项目概述、开发环境、目录结构、源码分层、启动流程、引脚与外设、PWM 输入采样、PWM 输出、应用逻辑、UART 通信、软件计时、看门狗、编译验证和开发注意事项。

在项目概述或工程环境附近增加醒目的“当前默认构建配置”，集中列出封装、芯片版本、输出模式和关键采样参数，避免读者从多个章节拼接当前状态。

## 准确性与验证

- 每个配置值必须能在当前工作区源码中定位。
- 文件名、宏名、函数名和 Keil 工程路径必须真实存在。
- 对无法在本环境完成的硬件验证和 Keil 编译明确标注“未执行”，不声称已经通过。
- 更新后使用文本搜索复核 README 中是否仍残留与当前配置冲突的 `TSSOP20 = 1`、`CHIP_9101`、8 次平均等表述。
- 仅检查和更新文档，不覆盖或整理用户已有的未提交代码改动。

## 完成标准

README 与当前工作区源码一致；新接手开发者可以从文档快速找到构建入口、默认配置、主流程和调试入口；Git 差异只包含预期的 README 文档更新以及本设计规格。
